//
//  checkpath.swift
//  MapApp
//
//  Created by George Wang on 10/20/24.
//


import SwiftUI
/*
struct ABC: View {
    var body: some View {
        Text("Finding file paths...") // Simple placeholder text
            .onAppear {
                // Find the paths for your files and print them
                if let stopsPath = Bundle.main.path(forResource: "stops", ofType: "txt") {
                    print("Real path for stops.txt: \(stopsPath)")
                } else {
                    print("stops.txt not found.")
                }

                if let tripsPath = Bundle.main.path(forResource: "trips", ofType: "txt") {
                    print("Real path for trips.txt: \(tripsPath)")
                } else {
                    print("trips.txt not found.")
                }

                if let routesPath = Bundle.main.path(forResource: "routes", ofType: "txt") {
                    print("Real path for routes.txt: \(routesPath)")
                } else {
                    print("routes.txt not found.")
                }

                if let shapesPath = Bundle.main.path(forResource: "shapes", ofType: "txt") {
                    print("Real path for shapes.txt: \(shapesPath)")
                } else {
                    print("shapes.txt not found.")
                }
            }
    }
}

*/
